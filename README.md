# encryption_algorithms

make, activate, install venv

```
python3 -m venv venv
source venv/bin/activate
pip3 install -r requirements.txt
```

Run

```
python3 app.py
```

Made boilerplate code

Uploaded files to decrypt shouldn't be too hard to figure out but its smth to do

dunno what to do for rsa